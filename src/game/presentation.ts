/**
 * Presentation toggle for the arcade screen.
 *
 * When true, the game renders inside the photoreal CRT television (desk scene)
 * with the curved WebGL shader. When false, the desk photo and curve are
 * dropped and the game fills a clean 4:3 screen (subtle scanlines kept for the
 * retro feel, matching how portrait/mobile already renders).
 */
export const SHOW_CRT_CABINET = false;
