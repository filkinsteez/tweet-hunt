/**
 * Gameplay feature flags.
 *
 * ENABLE_GOLDEN_DUCK_MODE: when true, Game A live-tweet rounds offer the duck
 * call that summons a golden duck for a one-shot mass-delete (Golden Flush).
 * When false, the duck call stays disabled and no golden duck can spawn.
 */
export const ENABLE_GOLDEN_DUCK_MODE = false;
